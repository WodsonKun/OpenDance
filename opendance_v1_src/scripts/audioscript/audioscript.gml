/// Handles all of the custom audio functions / scripts

/// @function audio_play_sfx(soundid)
/// @param sound
function audio_play_sfx(soundid) {
	audio_stop_sound(argument0);
	audio_play_sound(argument0,0,0);
}